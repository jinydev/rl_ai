**그림 5-21** 각 상태에서 개선과 평가를 순차적으로 반복

(그림 생략)

이번 그림에서는 상태 하나만 개선하고 곧장 평가 단계로 넘어갑니다. 평가 단계에서도 해당 상태 하나의 가치 함수를 한 번만 갱신합니다. 그런 다음 다른 위치(그림에서 왼쪽 위에서 두 번째 칸)를 개선하고 평가하는 흐름으로 진행됩니다.

NOTE_ [그림 5-21]에서는 먼저 왼쪽 위의 위치(상태)를 개선합니다. 지금 예에서는 개선 단계부터 시작했지만, 개선과 평가는 번갈아 진행되므로 어느 단계를 먼저 진행하든 상관없습니다.

[그림 5-21]의 아이디어를 수식으로 정리해보겠습니다. 개선 단계부터 시작하죠. 개선 단계에서 하는 탐욕화는 수식으로 다음과 같이 쓸 수 있습니다.

$$
\mu(s) = \operatorname{argmax}_a \sum_{s'} p(s' \mid s, a) \{ r(s, a, s') + \gamma V(s') \}
$$

[식 5.8]

현재의 가치 함수는 $V(s)$로 표기하겠습니다. [식 5.8]과 같이 현재 상태와 보상, 그리고 다음 상태를 활용하여 $\operatorname{argmax}$로 계산합니다. $\operatorname{argmax}$는 하나의 행동을 선택해주므로 $\mu(s)$와 같이 결정적 정책으로 표현할 수 있습니다.

5장 동적 프로그래밍 141
