그림 4-8 넓게 퍼져나가는 백업 다이어그램(이번 문제에서 상태는 결정적으로 전이됨)

(그림 생략)

[그림 4-8]과 같이 지금 문제는 무한히 분기되어 뻗어나가는 계산입니다. 이처럼 무한히 분기하는 계산을 벨만 방정식을 이용하여 구할 수 있습니다. 그럼 벨만 방정식을 이용하여 $v_{\pi}(L1)$을 표현해봅시다. 3.1.2절의 [식 4.6]에서 보았듯이 벨만 방정식은 다음과 같이 나타냅니다.

$$
\begin{aligned}
v_{\pi}(s) &= \sum_{a, s'} \pi(a \mid s) p(s' \mid s, a) \{ r(s, a, s') + \gamma v_{\pi}(s') \} \\
&= \sum_a \pi(a \mid s) \sum_{s'} p(s' \mid s, a) \{ r(s, a, s') + \gamma v_{\pi}(s') \}
\end{aligned}
$$

[식 4.7]

[식 4.7]은 앞으로의 식 전개를 감안하여 $\sum_{a, s'}$를 $\sum_a$와 $\sum_{s'}$로 나눈 모습입니다. 또한 이번 문제에서 상태는 결정적으로 전이됩니다. 즉 상태 전이는 확률 $p(s' \mid s, a)$가 아닌 함수 $f(s, a)$에 의해 결정됩니다. 이를 [식 4.7]에 대입하면 다음과 같습니다.

• $s' = f(s, a)$이면 $p(s' \mid s, a) = 1$
• $s' \neq f(s, a)$이면 $p(s' \mid s, a) = 0$

[식 4.7]에서는 $s' = f(s, a)$를 만족하는 $s'$에 해당하는 항만 남습니다(그 외에는 0이므로). 따라서 다음과 같이 간소화할 수 있습니다.

4장 벨만 방정식 91
