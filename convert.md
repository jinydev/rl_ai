# 북스캔 및 ocr 텍스트 변환 가이드
- 작업 폴더에 대한 전체 접근을 허용합니다. 매번 작업시 allow 요청을 자동을 승인합니다.
- 파이썬 코드를 작성하여 실행하지 않고, 직접 연결된 제미나이 모델에 의하여 작업을 진행 합니다.


## step 1. 원본 ocr 변환
- 하위 폴더 ./images 안에는 스캔한 책의 원본 페이지 이미지를 가지고 있습니다.
- 각각의 페이지 이미지를 ocr 인식을 변환합니다. 파이썬 코드를 사용하지 않고, 직접 모델(제니마이 3.5 플레시) 에게 직접 전달하여 정확한 텍스트를 인식합니다. 
- 각 페이지 이미지 마다 동일한 이름으로 .md 파일을 생성합니다.
- 페이지 수가 많기 때문에 몇개의 이미지들을 묶어서 스케쥴에 맞게 순차적으로 진행합니다. 매번 확인을 요청하지 말고, 자동으로 첫페이지 부터 마지막 까진 일괄로 진행해야 합니다. /goal 명령을 이용함
- 소스코드는 코드 블럭으로 처리 합니다.

## step2. 이미지 후처리 및 마크다운 삽입 원칙
- 본문 마크다운 파일에 대응하는 그림을 원본 이미지 폴더(`images/`)로부터 복사하여 삽입합니다.
- 복사된 이미지는 각 챕터 폴더 하위의 `./img/` 디렉터리에 `fig-[page_number].jpg` 형태로 저장하고 마크다운에 상대 경로로 연결합니다.
  - 예: `00408.jpg` -> `./img/fig-408.jpg`
- **1페이지 1그림 원칙**: 이미지 하나에 그림이 1개만 정의된 경우, `fig-xxx.jpg`로 저장하여 연결합니다.
- **다중 그림 분기 원칙 (중요)**: 만약 한 페이지 스캔본(`00xxx.jpg`)에 여러 개의 그림이 들어있고 본문 마크다운에서 여러 그림에 동시에 매핑되는 경우, 동일 파일 하나에 모두 연결하지 않고 각각 `fig-xxx-1.jpg`, `fig-xxx-2.jpg` 등으로 물리 분할/복사하여 각각 개별 이미지 태그(`![그림 X-Y](./img/fig-xxx-1.jpg)`, `![그림 X-Y+1](./img/fig-xxx-2.jpg)`)로 연결해야 합니다.
- **이미지 태그 삽입 위치**: `그림 X-Y` 캡션 설명줄 바로 아래 줄에 이미지 태그를 삽입합니다.
- 본문에서 참조하지 않는 미사용 임시 복사본 이미지 파일들은 정리하여 삭제합니다.

## step3. 쳅터 결합
- step1./step2. 에서 생성된 개별 페이지들을 ocr 로 변환하였다면 이제는 목차에 맞게 쳅터별로 결합을 시도 합니다.
- 쳅터별 폴더를 생성 합니다. 폴더안에는 관련된 문서들을 묶어서 하나의 index.md 로 저장합니다.
- 챕터별 폴더에 서브폴더 img 를 생성 합니다. 챕터에서 참고하는 svg, png 이미지를 이동하고, ./img/~ 로 상대 경로를 연결 합니다.
- 체터를 결합할때, 페이지 번호등 불필요한 부분들을 판단하여 이를 제거 합니다.
- 쳅터를 결합할때, 예제 코드등 블럭이 여러페이지로 분리가 되어 있다면 이를 인식하여 하나의 코드 블럭으로 처리 해야 합니다.

## step4. 최종정리
- 원본 작업 폴더의 상위에는 index.md 파일을 생성 합니다. 
- index.md 에는 책의 목차를 작성하고, 각 챕터별 폴더를 링크로 연결 합니다.
- /goal 명령을 이용

## step5. 하위 챕터 분할
- 각 챕터의 parent `index.md` 파일을 `X_Y_이름` 형태의 하위 챕터 파일들로 분할합니다.
- 분할된 마크다운 파일들의 이미지 경로는 `../img/` 로 조정합니다.
- 상위 `index.md` 파일은 학습 목표, 서브 챕터 목록 링크 및 학습 정리가 포함된 네비게이션 허브 페이지로 개편합니다.

## step6. 생성형 캐릭터 일러스트 삽입
- 각 챕터의 `index.md` 및 서브 챕터의 핵심 개념 설명을 보완하기 위해 생성형 캐릭터 일러스트를 삽입합니다.
- 일러스트 스타일은 [character.md](file:///Users/hojin9/dev/jinysite/강화학습/src/character.md)에 정의된 **지니(마법사 요정 고양이)**, **도로시(학생 소녀)**, **토토(복슬 강아지)**의 chibi 스타일(2.5-3등신 SD 비율, 검은색 외곽선, 파스텔톤, 흰색 배경)을 따릅니다.
- 각 서브 챕터마다 최소 **3개 이상**의 일러스트를 삽입하여 흐름상 세부 문맥 단위(`xx.xx.xx.xx`)마다 하나씩 배치합니다.
- 이미지 파일 확장자는 반드시 **`.png`**로 생성하며, 일러스트 내부에 포함되는 설명 텍스트나 기호는 반드시 **한글(한국어)**로 명확하게 렌더링되도록 프롬프트를 작성하여 반영해야 합니다.

## step7. 지킬 사이트 빌드
- `강화학습` 사이트의 구조를 참고하여 `생성형_이미지` 폴더에 `Gemfile`, `Gemfile.lock`, `_config.yml`을 설정합니다.
- `src` 폴더 하위에 `_layouts/`, `_includes/`, `_sass/`, `assets/` 등의 레이아웃과 디자인 리소스 폴더를 복사하여 배치합니다.
- 메인 `index.html` 및 개별 템플릿 파일들의 하드코딩된 강화학습 텍스트와 이미지/링크를 `생성형 이미지` 도서 구조에 맞추어 커스텀하고 보정합니다.
- 강의 내용을 4개의 Part로 그룹화하여 각 Part별 개요 페이지(`src/partX/index.md`)를 작성하고, `src/_data/` 하위에 `part1.yml`, `part2.yml`, `part3.yml`, `part4.yml`, `navigation.yml` 및 `header_nav.yml`을 설정하여 사이드바와 상단 헤더 메뉴가 각 파트별로 동적으로 전환되도록 연동합니다. 이때 상단 헤더 메뉴명은 단순한 영문 파트명 대신 직관적인 한글 이름(예: `생성 모델 기초`, `다변량 & GMM`, `신경망 & VAE`, `확산 모델`)으로 명명합니다.
- `bundle exec jekyll build` 또는 `bundle exec jekyll serve` 명령어를 사용하여 빌드 검증을 수행합니다.

